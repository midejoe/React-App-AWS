# React-App-AWS
Deploy A React App Into A Web Server

In your terminal, enter `terraform init` to initialize the recent version of AWS. The version used for this project is hashicorp/aws v4.48.0

Next, enter command `terraform apply --auto-approve`

For a more detailed explanation, check out my wiki TechChack
